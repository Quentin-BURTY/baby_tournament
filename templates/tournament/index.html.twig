{% extends 'base.html.twig' %}

{% block title %}Hello TournamentController!{% endblock %}

{% block body %}
   {% for tournaments in tournament %}
        <tr>
            <td>{{tournaments.name}}</td>
        </tr>
        {% else %}
         <tr>
            <td>Pas de tournois créé</td>
        </tr>
    {% endfor %}
        <td><a href="{{ path('tournament_new') }}">Créer un tournois</a></td>
{% endblock %}
